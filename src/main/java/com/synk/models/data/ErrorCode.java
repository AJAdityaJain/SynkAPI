package com.synk.models.data;

public enum ErrorCode {
    INVALID_PASSWORD,
    INVALID_SESSION,
    INVALID_EMAIL,
    INVALID_NAME,
    EXISTS,
    NOT_EXISTS,
    UNAUTHORIZED,
    ARGUMENT_NULL,
    UNKNOWN,
}
